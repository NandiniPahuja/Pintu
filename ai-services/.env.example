# Environment Configuration for PixMorph AI Service

# Server Config
HOST=0.0.0.0
PORT=5000
DEBUG=True

# Model Configuration
MODELS_DIR=./models
SAM_CHECKPOINT=sam_vit_h_4b8939.pth
SAM_MODEL_TYPE=vit_h
PIX2STRUCT_MODEL=google/pix2struct-base

# Device (auto-detected if not set: cuda or cpu)
# DEVICE=cuda

# OCR Configuration
OCR_BACKEND=paddleocr
# TESSERACT_CMD=/usr/bin/tesseract  # Uncomment and set if using Tesseract

# Processing Configuration
MAX_IMAGE_SIZE=2048
MIN_SEGMENT_AREA=100

# Upload Configuration
UPLOAD_FOLDER=./uploads
MAX_CONTENT_LENGTH=16777216  # 16MB in bytes

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
